# SDK drop-in directory

Download `captchala.aar` from <https://dash.captcha.la> and place it here:

```
app/libs/captchala.aar
```

The Gradle build picks it up automatically via the `flatDir` repository
declared in the project's `settings.gradle`.
